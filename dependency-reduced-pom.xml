<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.team360.hms</groupId>
  <artifactId>admissions-planner</artifactId>
  <version>0.0.1</version>
  <build>
    <resources>
      <resource>
        <directory>${project.resources.directory}</directory>
        <includes>
          <include>**/*</include>
        </includes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.7.0</version>
        <inherited>true</inherited>
        <configuration>
          <compilerArgs>
            <arg>-parameters</arg>
          </compilerArgs>
          <source>${maven.compile.source}</source>
          <target>${maven.compile.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <manifestEntries>
                    <Main-Class>${app.main.class}</Main-Class>
                  </manifestEntries>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>1.18.2</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>hamcrest-core</artifactId>
          <groupId>org.hamcrest</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.glassfish.jersey</groupId>
        <artifactId>jersey-bom</artifactId>
        <version>${jersey.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.jdbi</groupId>
        <artifactId>jdbi3-bom</artifactId>
        <version>${jdbi.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <properties>
    <jwt.version>3.4.0</jwt.version>
    <project.resources.directory>src/main/resources</project.resources.directory>
    <maven.compile.target>1.8</maven.compile.target>
    <liquibase.version>3.5.5</liquibase.version>
    <lombok.version>1.18.2</lombok.version>
    <argon2.version>2.4</argon2.version>
    <mysqlconnector.version>8.0.12</mysqlconnector.version>
    <app.main.class>com.team360.hms.admissions.Main</app.main.class>
    <gson.version>2.8.5</gson.version>
    <jdbi.version>3.4.0</jdbi.version>
    <guice.version>4.2.0</guice.version>
    <logger.version>2.11.1</logger.version>
    <commons-text.version>1.4</commons-text.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <simplejavamail.version>5.0.3</simplejavamail.version>
    <junit.version>4.12</junit.version>
    <maven.shade>3.2.0</maven.shade>
    <hikaricp.version>3.2.0</hikaricp.version>
    <guava.version>26.0-jre</guava.version>
    <maven.compile.source>1.8</maven.compile.source>
    <json.version>20180813</json.version>
    <jersey.version>2.27</jersey.version>
    <emailaddress.version>1.1.0</emailaddress.version>
    <commons-lang.version>3.8</commons-lang.version>
  </properties>
</project>
